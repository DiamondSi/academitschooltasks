package ru.academitschool.lesson10.homework.course;

// 10.33
public class BinarySearch {
    public static int findElemetIndex(double element, int left, int right, double array[]) {
        if (left>right){
            return -1;
        }else
        if (element > array[middle]) {
            left = middle + 1;
            findElemetIndex(double element, int lefr, int right, double array[]);
        }
    }
}
